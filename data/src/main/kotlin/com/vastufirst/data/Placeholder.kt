package com.vastufirst.data

/**
 * Phase 0 placeholder.
 *
 * Phase 2 fills this module with the SQLDelight schema (plans / analyses / reports),
 * repositories, and the Supabase client. Every stored analysis carries its
 * `ruleSetVersion`; saved analyses are never silently re-scored under new rules
 * (Product PRD §5) — a re-run is offered instead.
 */
internal const val DATA_MODULE = "vastufirst-data"
