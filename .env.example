# Copy to .env and fill in. .env is git-ignored; this example is committed.
# Groq (free tier) — https://console.groq.com → API Keys
GROQ_API_KEY=
# OpenRouter (paid fallback) — https://openrouter.ai/keys
OPENROUTER_API_KEY=
