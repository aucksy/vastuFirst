# Render every screen against Android 15 (SDK 35) — our targetSdk. Robolectric 4.14.1 is the
# first release able to render SDK 35; an older SDK here would silently render on a platform the
# app never targets. Required alongside testOptions.unitTests.isIncludeAndroidResources = true.
sdk=35
